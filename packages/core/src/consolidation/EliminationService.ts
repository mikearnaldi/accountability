/**
 * EliminationService - Re-export from canonical location
 *
 * This file provides the new import path for EliminationService
 * while maintaining backward compatibility during the core package reorganization.
 *
 * @module consolidation/EliminationService
 */

export {
  EliminationService
} from "../Services/EliminationService.ts"
